<?php

use Illuminate\Routing\Router;
use Illuminate\Support\Facades\Route;
use Dcat\Admin\Admin;

Admin::routes();

Route::group([
    'prefix'        => config('DummyApp.route.prefix'),
    'namespace'     => config('DummyApp.route.namespace'),
    'middleware'    => config('DummyApp.route.middleware'),
], function (Router $router) {

    $router->get('/', 'HomeController@index');

});
